# Frontend State Logic

- Use a `useEditor` hook to manage the input/output sync.
- Loading State: Show a pulse animation on the "Corrected" box while the Flask API is "Pending."
- Error State: Store backend error messages in a global "Notification" state.
