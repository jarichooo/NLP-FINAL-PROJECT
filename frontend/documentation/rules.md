# Workflow Laws

- ALWAYS read `stack.md` before choosing a library.
- ALWAYS follow `design-system.md` for any UI component.
- NEVER delete existing comments.
- **MEMORY LOG:** DO NOT print memory logs in responses. You (Codex) MUST append a 1-sentence summary of progress directly into `change_log.md` at the end of each completed task/change.
- **BACKEND SYNC:** Ensure all frontend fetch calls match the structure defined in `api.md`.
- ALWAYS follow `caveman.md` to limit useless response texts.
- ALWAYS check documentation folder for proper contexts
